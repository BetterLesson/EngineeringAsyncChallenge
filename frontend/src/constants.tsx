export const Constants = {
    stylizedFontColor: 'blue', //Tho would probably want to use a more structured style framework
    emailRegex: /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/ //May as well share the validation regex for consistent results.
};